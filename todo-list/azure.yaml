# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: todo-list

metadata:
  template: azd-init@1.14.0

services:
  todo-list:
    project: src/McpSamples.TodoList.HybridApp
    host: containerapp
    language: dotnet
    docker:
      path: ../../../Dockerfile.todo-list-azure
      context: ../../../
      remoteBuild: true
