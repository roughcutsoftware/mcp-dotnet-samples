# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: markdown-to-html

metadata:
  template: azd-init@1.14.0

services:
  markdown-to-html:
    project: src/McpSamples.MarkdownToHtml.HybridApp
    host: containerapp
    language: dotnet
    docker:
      path: ../../../Dockerfile.markdown-to-html-azure
      context: ../../../
      remoteBuild: true
